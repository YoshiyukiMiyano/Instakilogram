<h1><%= @user.name %>のページ</h1>

  <p>メールアドレス: <%= @user.email %></p>
  <%= image_tag @user.profile_image %>


    <div class="row"></div><h2>お気に入り一覧</h2>
    <% @favorites_pictures.each do |favorite| %>
      <% @favorite =  Favorite.find(favorite.id) %>

      <div class="col-xs-12 col-md-6 blog-panel2">
        <h2><%= @favorite.picture.image %></h2>
        <p><%= @favorite.picture.content %></p>
        <p><%= @favorite.picture.created_at %></p>
      </div>
    </div>
    <% end %>
